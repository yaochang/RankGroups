class InputFileFormat {
    /*
     *  First need to make sure the input file format, then we can do the
     *  processing  First need to make sure the input file format, then we can
     *  do the processing..
     * */
}
